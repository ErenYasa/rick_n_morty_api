import { Container, Spinner } from '../../styles/components/Misc/LoaderStyle';

export default function Loader() {
    return (
        <Container>
            <Spinner />
        </Container>
    );
}
